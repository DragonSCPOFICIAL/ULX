#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int a = 10;
    int b = 5;
    printf("Soma: %d\n", (a + b));
    printf("Subtração: %d\n", (a - b));
    printf("Multiplicação: %d\n", (a * b));
    printf("Divisão: %d\n", (a / b));
    return 0;
}